#include <stdio.h>
#include <ctype.h>

int main()
{
    char vogais = 'a';//, 'e', 'i', 'o', 'u';
    int texto [40];
    vogais = toupper(vogais);
    printf("vogais: %c", vogais);
          return 0 ;
}